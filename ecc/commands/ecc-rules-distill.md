---
description: rules-distill スキルへの互換エントリポイント（レガシー）。通常はスキルを直接使うことを推奨。
---

# Rules Distill (Legacy Shim)

Use this only if you still invoke `/ecc-rules-distill`. The maintained workflow lives in `skills/rules-distill/SKILL.md`.

## Canonical Surface

- Prefer the `rules-distill` skill directly.
- Keep this file only as a compatibility entry point.

## Arguments

`$ARGUMENTS`

## Delegation

Apply the `rules-distill` skill and follow its inventory, cross-read, and verdict workflow instead of duplicating that logic here.
